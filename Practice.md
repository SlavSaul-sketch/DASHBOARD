# Practice - כללי עבודה בפרויקט הדאשבורד

איך עובדים על הפרויקט הזה: מה מותר, מה אסור, איך משנים, ואיך בודקים.
מה בונים מוגדר ב-`SPEC.md`. איך מרעננים ומחשבים מוגדר ב-`הוראות-דאשבורד.md`.

---

## 1. עקרונות

1. **הנתונים של שליו הם קריאה בלבד.** לא כותבים, לא עורכים ולא מוסיפים טאב, הערה או רשומה בגיליונות וב-Airtable. גם לא "רק לבדיקה". הדף מקבל גישה לכלי קריאה אחד בלבד של Airtable (`list_records_for_table`), ולא מוסיפים לו כלי כתיבה
2. **מספר שגוי יקר יותר ממספר חסר.** אם הנתונים חשודים, מציגים התראה ולא מספר. לא ממציאים שיוך ולא מנחשים
3. **אפס שעות עבודה לשליו.** כל פתרון שדורש ממנה פעולה ידנית קבועה הוא פתרון גרוע
4. **אותה מתודולוגיה בכל הרצה**, גם כשהתוצאה פחות מחמיאה. שינוי בהגדרה נרשם במסמכים לפני שהוא נכנס לקוד
5. **פרטיות כברירת מחדל.** פרטים מזהים לא יוצאים מהמחשב, לא נכנסים לדאשבורד ולא נכנסים ל-GitHub

## 2. סדר עדיפויות כשיש סתירה

1. מה ששליו אמרה במפורש בשיחה
2. `SPEC.md`
3. `הוראות-דאשבורד.md`
4. `Practice.md` (הקובץ הזה)
5. הקוד

כשהקוד לא תואם למסמכים, מתקנים את הקוד. כשהמסמכים לא תואמים למציאות, מעדכנים אותם ומציינים זאת לשליו.

## 3. תהליך שינוי

כל שינוי בדאשבורד, קטן או גדול, עובר את השלבים האלה:
1. **הגדרה:** מה משתנה ולמה. אם זו הגדרה עסקית (מקור ליד, שיוך, נוסחה), מעדכנים קודם את `הוראות-דאשבורד.md`
2. **מימוש:** הגדרות וחישובים ב-`build/dashboard.template.html`. קריאה ונרמול נתונים ב-`build/build-data.ps1`. אין לערוך את `dashboard.html` ידנית, הוא נבנה מחדש בכל רענון
3. **בנייה:** הרצת נוהל הרענון (סעיף 8 בהוראות)
4. **בדיקה:** לפי סעיף 5 כאן
5. **פרסום** לאותה כתובת Artifact
6. **תיעוד:** commit עם הסבר, ועדכון `SPEC.md` אם השתנה ההיקף או הדרישות

## 4. כללי קוד

### PowerShell
- PowerShell 5.1 בלבד. אין Python, Node או אקסל במחשב, ולא מוסיפים תלויות
- קבצי `.ps1` נשמרים ב-**UTF-8 עם BOM**. בלי BOM, PowerShell 5 קורא עברית כג'יבריש ונכשל
- טאבים מאותרים לפי **שם**, לא לפי מיקום. טאב חסר עוצר את הבנייה עם הודעה ברורה
- עמודות מאותרות לפי שם הכותרת. כותרת כפולה מקבלת סיומת (`_2`)

### תאריכים
- בגיליונות יש כמה פורמטים במקביל: מספר סידורי של אקסל, `dd/mm/yy HH:mm`, `dd/mm/yyyy`, `dd-mm-yyyy`, ופורמט אמריקאי ישן עם AM/PM. הפונקציה `ToDate` מטפלת בכולם, וכל פורמט חדש מתווסף אליה
- ב-JavaScript לא משתמשים ב-`new Date()` עם שעון מקומי לחישובי חודשים. ההמרה ל-UTC מזיזה יום אחורה ויוצרת חודש מיותר. עובדים עם מחרוזות `yyyy-MM` או עם `Date.UTC`

### זהות
- טלפון: רק ספרות, בלי 972 ובלי אפס מוביל. מספרים שנשמרו בכתיב מדעי (`9.72E+11`) מומרים חזרה למספר מלא
- מייל: trim ו-lowercase
- המפתח המוצפן נוצר רק ב-`build-data.ps1`. הדאשבורד לא רואה את הערך המקורי

### תבנית הדאשבורד
- כל הצבעים מוגדרים כמשתנים ב-`:root`, עם גרסה למצב כהה. אין צבע קשיח ברכיב
- צבע קבוע לכל מקור בכל הגרפים: מטא `#9BDAD9`, אורגני גרפיט, שותפים אפור בהיר
- צבעי סטטוס (ירוק, ענבר, אדום) רק לסטטוס, ותמיד עם טקסט
- מספרים בתוך טקסט עברי עטופים ב-`<bdi>`, ובעמודות מספרים יש `tabular-nums`
- טבלה רחבה גוללת בתוך המיכל שלה. הדף עצמו לא גולל לרוחב
- כשמרכיבים מחרוזות HTML בתוך JavaScript, לא משתמשים ב-`\'` מחוץ למחרוזת. זה שובר את כל הדף

### סנכרון Airtable בדף
- הדף ניגש ל-Airtable דרך הקונקטור של claude.ai (claude.use("mcp")), עם ההרשאות של מי שצופה בדף. אין בקוד מפתחות או טוקנים
- כל קוד שגיאה מקבל הודעה משלו עם הפעולה שמתקנת אותו. לא מכווצים הכל ל"משהו השתבש"
- ניסיון חוזר רק לשגיאה שמסומנת retryable, פעם אחת, אחרי המתנה
- תוצאת הסנכרון נשמרת במסמך אחד (sync/meta) ונכתבת רק אחרי לחיצה, לא בטעינה
- בקובץ המקומי אין window.claude, ולכן הכפתור מוסתר שם. זה תקין

### זיהוי מודעות
- `NEWCarusela` נבדק **לפני** `Carusela`
- סרטון הקרוסלה שבאד סט הוותיק שייך לקבוצה הוותיקה
- מודעה חדשה נוספת לטבלה בהוראות (סעיף 4.2) ולפונקציה `groupOfText`. עד אז היא מוצגת כ"מודעה לא מזוהה"

## 5. בדיקה לפני פרסום

1. הבנייה הסתיימה בשורת `OK` וההתראות (`WARN:`) נקראו
2. קריטריוני הקבלה ב-`SPEC.md` סעיף 7 מתקיימים. בפרט: חודשים סגורים מול הטאב דוח חודשי
3. מבט אחד בדף (צילום מסך או טקסט הדף) כדי לוודא שהוא נטען ושאין שגיאות בקונסול
4. אם מספר השתנה בחדות מאז הרענון הקודם, בודקים קודם את הנתונים ורק אחר כך מסיקים מסקנה עסקית

## 6. לקחים שכבר נלמדו

| מה קרה | הכלל |
|---|---|
| `read_file_content` של קונקטור הדרייב החזיר כ-100 שורות לטאב בלי להתריע | קוראים רק דרך `download_file_content` כ-xlsx |
| ייצוא CSV ישיר מהדרייב מחזיר רק את הטאב הראשון | ממירים את ה-xlsx לכל הטאבים עם `xlsx2csv.ps1` |
| אקסל דרך COM נתקע על טאבים נסתרים ועל קבצים גדולים | לא משתמשים באקסל. קוראים את ה-XML של ה-xlsx ישירות |
| חודש מאי הופיע בדאשבורד בגלל המרת אזור זמן | ראו כלל התאריכים בסעיף 4 |
| הנוסחה "סהכ עלויות" בדוח החודשי כוללת את "עלות ליד" | הדאשבורד מחשב את סך ההוצאות בעצמו ומציג את הפער |
| הלינק לטאב האורגני הצביע על טאב נסתר וישן | מאמתים שהטאב מכיל נתונים עדכניים לפני שמשתמשים בו |

## 7. Git ו-GitHub

- ריפו פרטי: https://github.com/SlavSaul-sketch/DASHBOARD. **לא הופכים אותו לציבורי**, כי הדאשבורד מכיל נתונים עסקיים
- נכנסים לריפו: המסמכים, `build/` ו-`dashboard.html`
- **לא נכנסים לריפו:** קובצי xlsx ו-CSV גולמיים, דוחות מטא, תשובות Airtable שמורות, וכל קובץ עם שמות, טלפונים או מיילים. הם נשארים בתיקייה הזמנית ונמחקים בסוף הרענון
- הודעת commit מסבירה מה השתנה ולמה
- push רק באישור של שליו, או כחלק מרענון שהיא אישרה

## 8. תקשורת עם שליו

- עברית, קצר וברור, בלי מקף ארוך
- יועץ בכיר, לא מרצה. לא מסכימים אוטומטית. אומרים בישירות כשמשהו לא עובד או לא משתלם
- שניים עד שלושה ממצאים או מהלכים, בסדר עדיפות, ומה לעשות קודם. לא רשימות ארוכות
- כל המלצה נמדדת לפי: גידול בהכנסות, רווחיות, יחס מאמץ-תוצאה, סקיילביליות ואוטומציה
- לא ממליצים לעצור או להגדיל מודעה על סמך יום אחד או פחות מ-5 רוכשות
- כל פעולה שמשנה משהו מחוץ לתיקייה (פרסום, push, משימה מתוזמנת, שיתוף) דורשת אישור מפורש
